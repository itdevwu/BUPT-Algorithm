#include "merge_sort.hpp"

lab2::utils::SortInfo lab2::merge_sort::sort(std::vector<int> &vec)
{
    lab2::utils::SortInfo sort_info;

    const auto qsort_f = [&]()
    {
        
    };

    qsort_f();

    return sort_info;
}